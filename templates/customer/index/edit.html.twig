{% from "@components/macros/content.html.twig" import header %}

{% extends '@base/app.html.twig' %}

{% block title %}Modification du client{% endblock %}

{% block body %}
    {{ header({
        text: 'Modification du client',
        johan: 'bg-warning-400',
    }) }}

    <div class="p-4">
        <div class= "flex flex-row items-center justify-start mb-4 text-xs text-secondary-500 hover:text-primary-500 ease-in-out">
            <i class="fa-solid fa-arrow-left mr-1"></i>
            <a href="{{ path('app_customer_index') }}">Retour à la liste</a>
        </div>
        {% embed "@components/container/form-container.html.twig" %}
            {% block content %}
                {{ include('@customer/index/_form.html.twig', {
                    button_label: 'Modifier',
                    button_classes: [
                        'bg-warning-400',
                        'text-secondary-900',
                        'hover:bg-warning-500',
                    ]
                }) }}
            {% endblock %}
        {% endembed %}
        </div>
    </div>
{% endblock %}
