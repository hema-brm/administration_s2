{% extends '@base/app.html.twig' %}
{% from "@components/macros/content.html.twig" import header %}

{% block title %}Facture{% endblock %}


{% block body %}
    {{ displayFlash() }}
    {{ header({
        text: 'Modifier la facture',
    }) }}

    <div class= "flex flex-row items-center justify-between mb-4 m-4 text-xs text-secondary-500 hover:text-primary-500 ease-in-out">
        <div>
            <i class="fa-solid fa-arrow-left mr-1"></i>
            <a href="{{ path('app_bill_index') }}">Retour à la liste</a>
        </div>
        <div>
            <twig:Button
                text="Afficher"
                color="secondary"
                route="{{ path('app_bill_show', {'id': bill.id}) }}"
                icon="fa-pen"
            />
        </div>
    </div>

    <div class="px-4">
        <twig:BillCreator :bill="bill" mode="edit" />
    </div>
{% endblock %}
