{% extends '@base/app.html.twig' %}
{% from "@components/macros/content.html.twig" import header %}

{% block title %}Suivis de paiement{% endblock %}

{% block body %}
{{ header({
        text: 'Ajouter un paiement',
}) }}
<div class="items-center justify-between mb-4 m-4 text-xs text-secondary-500 hover:text-primary-500">
    <div>
          <i class="fa-solid fa-arrow-left mr-1 mb-4"></i>
          <a href="{{ path('app_bill_index') }}">Retour à la liste</a>
    </div>
    <div class="w-full md:w-2/3 lg:w-1/2 bg-secondary-100 p-8 rounded-lg shadow-md">
        {{ include('payment/_form.html.twig') }}
    </div>
</div>
{% endblock %}
