{% import "@components/navigation/menu/macro/index.html.twig" as menuMacro %}

<aside data-open="true" class="overflow-hodden nav relative bg-red-100 text-bg-stone-800 w-72 ml-0 transform min-h-screen transition-all duration-500 ease-in-out shadow-2xl overflow-y-visible">
    <ul class="nav-item">
        {{ menuMacro.item({
            text: 'Factures',
            icon: 'fa-file-invoice-dollar',
            route: path('app_bill_index'),
        }) }}
        {{ menuMacro.item({
            text: 'Devis',
            icon: 'fa-file-invoice',
            route: path('app_quote_index')
        }) }}
        {{ menuMacro.item({
            text: 'Clients',
            icon: 'fa-user',
            route: path('app_customer_index')
        }) }}
        {{ menuMacro.item({
            text: 'Paiements',
            icon: 'fas fa-money-bill-wave',
            route: path('app_payment_index')
        }) }}
        
        {{ menuMacro.item({
            text: 'Produits',
            icon: 'fa-box',
            route: path('app_product_index')
        }) }}
    </ul>
</aside>

<script>
    window.addEventListener('load', () => {
        (new NavToggle('#btn_toggle-menu', '.nav'));
    });
</script>