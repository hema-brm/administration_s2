{% import "@components/navigation/menu/macro/index.html.twig" as menuMacro %}

<aside data-open="true" class="overflow-hodden nav relative bg-cyan-900 text-white w-72 ml-0 transform min-h-screen transition-all duration-500 ease-in-out shadow-2xl overflow-y-visible">
    <ul class="nav-item">
        {{ menuMacro.item({
            text: 'Factures',
            icon: 'fa-file-invoice'
        }) }}
        {{ menuMacro.item({
            text: 'Clients',
            icon: 'fa-user',
            route: path('app_customer_index')
        }) }}
        {{ menuMacro.item({
            text: 'Paiements',
            icon: 'fas fa-money-bill',
            route: path('app_payment_index')
        }) }}
        
        {{ menuMacro.item({
            text: 'Produits',
            icon: 'fa-box',
            route: path('app_product_index')
        }) }}
    </ul>
</aside>

<script>
    window.addEventListener('load', () => {
        (new NavToggle('#btn_toggle-menu', '.nav'));
    });
</script>