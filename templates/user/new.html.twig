{% from "@components/macros/content.html.twig" import header %}

{% extends '@base/app.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
     {{ header({
        text: 'Ajouter un employé',
    }) }}

    <div class="p-4">
        <div class= "flex flex-row items-center justify-start mb-4 text-xs text-gray-500 hover:text-blue-500 ease-in-out">
            <i class="fa-solid fa-arrow-left mr-1"></i>
            <a href="{{ path('app_user_index') }}">Retour à la liste</a>
        </div>
        <div class="p-4 shadow-md w-1/2 border rounded">
            <div class="text-sm">
                {% include('user/_form.html.twig') with {
                    button_label: 'Ajouter',
                    button_classes: [
                        'bg-blue-500',
                        'text-white',
                        'hover:bg-blue-600',
                        'ml-2',
                    ]
                } %}
            </div>
        </div>
        </div>
        
    </div>
{% endblock %}
